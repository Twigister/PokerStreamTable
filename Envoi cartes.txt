import obswebsocket
from obswebsocket import requests as obs_requests

host = "192.168.0.35"
port = 4444
passw = "obscapue"
// ♣♦♥♠
socket = obswebsocket.obws(host, port, passw)
socket.connect()

text_data = { "text": "A♠ A♣", "source": "Cartes1" }

socket.call(obs_requests.SetTextGDIPlusProperties(**text_data))

socket.disconnect()